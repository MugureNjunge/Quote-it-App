import { Quote } from './quote';

describe('Quote', () => {
  it('should create an instance', () => {
    expect(new Quote(id: number, author: string, createdBy: string, statement: string, createdAt: Date, likes ?: number, dislikes ?: number): Quote
import Quote{ }

    Quote()).toBeTruthy();
});
});